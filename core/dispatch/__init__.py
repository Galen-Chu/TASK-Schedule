"""Optional output dispatch (Google Drive / Gmail).

These modules expose a clean interface but are stubs: they log and no-op
unless Google credentials are configured. Wire ``google-api-python-client`` in
here when ready to go live.
"""
